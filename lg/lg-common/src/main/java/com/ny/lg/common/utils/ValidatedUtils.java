package com.ny.lg.common.utils;

/**
 * 分组接口类只是普通的接口类并没有多大意义，
 * 只是用来标识这个属性哪种情况下被验证
 */
public class ValidatedUtils {

    public interface add{};

    public interface select{};

    public interface update{};
}